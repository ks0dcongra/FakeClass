<header>
  <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark px-4">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">上課假視訊產生器</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
        aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      {{#if user}}
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav ms-auto mb-2 mb-md-0">
          {{#if user.isAdmin}}
          <li class="nav-item">
            <a href="/admin/fake-class" class="nav-link">管理員後台</a>
          </li>
          {{/if}}
          <li class="nav-item d-inline-flex align-items-center">
            <span class="text-white nav-link">您好，{{ user.email }}</span>
          </li>
          <li class="nav-item">
            <a href="/logout"><button class="btn btn-outline-success my-sm-0 my-2">登出</button></a>
          </li>
        </ul>
      </div>
      {{else}}
      <ul class="navbar-nav ms-auto mb-2 mb-md-0">
        <li class="nav-item me-2">
          <a href="/logout"><button class="btn btn-outline-success my-sm-0 my-2">註冊</button></a>
        </li>
        <li class="nav-item">
          <a href="/signin"><button class="btn btn-outline-success my-sm-0 my-2">登入</button></a>
        </li>
      </ul>
      {{/if}}
    </div>
  </nav>
</header>